import { AdminLayout } from '@/components/Layouts';import { listRequests } from '@/lib/data';
export default async function Page(){const rows=await listRequests();return <AdminLayout><h1 className="text-3xl font-black text-agro-dark">Produtores</h1><div className="mt-6 overflow-hidden rounded-3xl bg-white shadow-soft"><table className="w-full text-sm"><tbody>{rows.map(r=><tr className="border-b" key={r.id}><td className="p-3 font-bold">{r.producer_name}</td><td>{r.producer_whatsapp}</td><td>{r.city}/{r.state}</td><td>{r.crop}</td><td>{r.area_amount} {r.area_unit}</td><td>public_form</td></tr>)}</tbody></table></div></AdminLayout>}
